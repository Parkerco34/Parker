import tkinter as tk

# Initialize the window manager:
'''
window = tk.Tk()
window.title("GUI")
'''
# Label is used to insert some objects into the window
# pack is used to show the object in the window:
#label = tk.Label(window, text='This is pathetic').pack()
# mainloop() method to display the window until you manually close it...

# To arrange the layout in the window, we will use Frame; You can align the frames as you like with side parameter of pack() method
# Button is used to create a button in the window. It takes several parameters like text(Value of the Button), 
# fg(Color of the text), bg(Background color)

# creating 2 frames TOP and BOTTOM:
'''
topFrame = tk.Frame(window).pack()
bottomFrame = tk.Frame(window).pack(side='bottom')
#create some widgets in the top_frame and bottom_frame:
btn1 = tk.Button(topFrame, text='Button1', fg='red').pack()
btn2 = tk.Button(topFrame, text='Button2', fg='green').pack()
btn3 = tk.Button(bottomFrame, text='Button3', fg='purple').pack(side='left')
btn4 = tk.Button(bottomFrame, text='Button4', fg='orange').pack(side='left')
window.mainloop()
'''

#
'''
tk.Label(window, text='Sufficient Width', fg="white", bg='purple').pack()
# width of X:
tk.Label(window, text='Taking all available x width', fg='white', bg='green').pack(fill="x")
# height of Y:
tk.Label(window, text='Taking all available y height', fg='white', bg='black').pack(side='left',fill='y')
window.mainloop()
'''
# GRID:
'''
# creating 2 text labels and input labels:
tk.Label(window, text='Username').grid(row=0) # placed in 0 0
# 'Entry' is used to display the input-field:
tk.Entry(window).grid(row=0,column=1)
tk.Label(window, text='Password').grid(row=1)
tk.Entry(window).grid(row=1,column=1)
# 'columnspan' tells to take the width of 2 columns; can also use rowspan**
tk.Checkbutton(window, text='Keep me logged in').grid(columnspan=2)
window.mainloop()
'''
# Binding Funtions
'''
# creating a function called say_hi():
window = tk.Tk()
window.title('GUI')

def say_hi(event):
    tk.Label(window, text='HI').pack()
# 'command' is executed when you click the button:
btn = tk.Button(window, text='Click Me!')
btn.bind("<Button-1>", say_hi) # 'bind' takes 2 parameters 1st is 'event' 2nd is 'function'
btn.pack()

window.mainloop()
'''
# Mouse Clicking Events
